apiVersion: v1
kind: ConfigMap
metadata:
  name: garmin-uploader-config
  namespace: {{ .Values.global.namespace }}
data:
  appsettings.Production.json: |
    {
      "Logging": {
        "LogLevel": {
          "Default": "{{ .Values.logging.level.default }}",
          "Microsoft.Hosting.Lifetime": "{{ .Values.logging.level.microsoft }}",
          "Microsoft.EntityFrameworkCore": "{{ .Values.logging.level.entityFramework }}"
        }
      },
      "ConnectionStrings": {
        "kafka": "{{ .Values.kafka.bootstrapServers }}",
        "redis": "{{ .Values.redis.host }}:{{ .Values.redis.port }}"
      },
      "GarminUploaderOptions": {
        "InstanceId": "{{ .Values.garmin.config.instanceId }}",
        "HeartbeatIntervalMinutes": {{ .Values.garmin.config.heartbeatIntervalMinutes }},
        "MaxRetries": {{ .Values.garmin.config.maxRetries }},
        "OrphanThresholdMinutes": {{ .Values.garmin.config.orphanThresholdMinutes }},
        "RateLimits": {{ .Values.garmin.config.rateLimits | toJson }}
      }
    }
